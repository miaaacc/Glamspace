// src/theme.js
// Los colores usan variables CSS definidas en index.css (claro y oscuro).
export const C = {
  pink:      "var(--pink)",
  pinkLight: "var(--pink-light)",
  pinkMid:   "var(--pink-mid)",
  pinkDark:  "var(--pink-dark)",
  beige:     "var(--beige)",
  beigeCard: "var(--beige-card)",
  text:      "var(--text)",
  muted:     "var(--muted)",
  border:    "var(--border)",
  white:     "var(--white)",
}

export const FONT = {
  display: "'Playfair Display', Georgia, serif",
  body:    "'DM Sans', 'Helvetica Neue', sans-serif",
}
